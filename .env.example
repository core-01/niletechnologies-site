# Set to "false" once a real backend is ready; the API layer under /src/services
# will automatically switch from mock data to real HTTP calls.
VITE_USE_MOCK_API=true

# Base URL for the real backend (used only when VITE_USE_MOCK_API=false)
VITE_API_BASE_URL=http://localhost:4000/api
